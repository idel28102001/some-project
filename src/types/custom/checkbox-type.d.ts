import { BaseType } from "@/types/basic/base-type";
import { MaterialType } from "@/types/basic/material-type";

export interface CheckboxType extends BaseType {
  checked: boolean;
}

export interface CheckboxType2 extends MaterialType {
  checked: boolean;
}
